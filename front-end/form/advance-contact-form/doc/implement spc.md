# 高级联系表单

## 页面内容

表单由三部分组成，使用`<fieldset>`标签进行分组。第一部分是用户基本信息，第二部分中，每个checkbox占据fieldset 33%的宽度。最后的提交按钮在fieldset之外。

## 页面布局

三个checkbox以行内元素方式显示，宽度设为33%均分fieldset的宽度，向左浮动后，checkbox间距自动加宽，这是浮动样式默认的属性。

最后的提交按钮左外边距设置为20px与fieldset内边距对齐。